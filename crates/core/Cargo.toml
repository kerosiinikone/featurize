[package]
name = "featurize-core"
version = "0.1.0"
edition = "2021"

[dependencies]
num-traits = { version = "0.2", default-features = false, features = ["libm"] }

[dev-dependencies]
trybuild = "1.0"

[[test]]
name = "compile_fail"
harness = false
